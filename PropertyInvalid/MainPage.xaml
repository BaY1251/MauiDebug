<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
			 xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
			 xmlns:local="clr-namespace:PropertyInvalid"
			 x:Class="PropertyInvalid.MainPage"
			 x:DataType="local:MainPage">

	<ScrollView>
		<VerticalStackLayout Padding="30,0"
							 Spacing="25">
			<Button x:Name="CounterBtn"
					Text="Click me"
					SemanticProperties.Hint="Counts the number of times you click"
					Clicked="OnCounterClicked"
					HorizontalOptions="Fill" />

			<VerticalStackLayout>
				<Switch IsToggled="{Binding Path=List[0-7]}" />
				<Switch IsToggled="{Binding Path=List[0-6]}" />
				<Switch IsToggled="{Binding Path=List[0-5]}" />
				<Switch IsToggled="{Binding Path=List[0-4]}" />
				<Switch IsToggled="{Binding Path=List[0-3]}" />
				<Switch IsToggled="{Binding Path=List[0-2]}" />
				<Switch IsToggled="{Binding Path=List[0-1]}" />
				<Switch IsToggled="{Binding Path=List[0-0]}" />
			</VerticalStackLayout>
		</VerticalStackLayout>
	</ScrollView>

</ContentPage>
